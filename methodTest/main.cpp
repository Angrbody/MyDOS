#include"student.h"
#include<iostream>
#include<string>
#include<vector>
using namespace std;
int main(){
    student A("小明", 0);
    // A.kaoshi();
    std::vector<student*> Aclass;
    student* stu = new gaoyi;
    Aclass.push_back(stu);
    system("pause");
    return 0;
}